apiVersion: v1
kind: Service
metadata:
  name: {{ include "hotify-bark-server.fullname" . }}
  labels:
    {{- include "hotify-bark-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hotify-bark-server.selectorLabels" . | nindent 4 }}
