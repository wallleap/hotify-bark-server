apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hotify-bark-server.fullname" . }}-test-connection"
  labels:
    {{- include "hotify-bark-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hotify-bark-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
